/node_modules
/dist
/out-tsc
/.angular
/test-results
/playwright-report
# Screenshots: the ui-harness leaves one per failing layout check here, and
# `pnpm run shots` writes one per screen deliberately. Neither is committed —
# they are pictures for a person to look at, and nothing asserts on them, so a
# committed one would be a baseline nobody is comparing against.
/ui-snapshots/
